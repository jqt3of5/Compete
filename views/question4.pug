div
    | A Hamiltonian Path through a graph is a path that touches each node exactly once. 
    a(href='http://mathworld.wolfram.com/HamiltonianCycle.html') Wiki.
    | It is possible for some graphs to contain no Hamiltonian path, if it is not possible to traverse the graph without visiting the same node twice. 

    br
    br
    div(style='text-align:center')
        b Given an adjacency matrix, find the length of the shortest hamiltonian path. Edge lengths of -1 mean the nodes are not connected. The answer should be -1 if the graph does not contain a hamiltonian path. 
    br
    div
        div Example:
            div(style='margin:0 auto; width:100px')
                span -1 1 3 -1
                br
                span 1 -1 1 4
                br
                span 3 1 -1 2
                br
                span -1 4 2 -1
        div 
            span Shortest Hamiltonian Path: A -> B -> C -> D
            br
            span Answer: 4
            br
            br


div Challenge: 
        div(style='margin:0 auto; width:300px')
            each row in matrix
                each element in row
                    span= element + " "
                br

			   
form(id='answer-form')
    label(for='answer') length = 
    input(type='text', id='answer')
    input(type='submit',id='submit-answer',value='Submit') 
